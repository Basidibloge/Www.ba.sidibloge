name: Global-Hunter
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Hunting Servers
        run: |
          # هذا الرابط هو الأحدث حالياً لجلب سيرفرات IPTV و Xtream
          curl -sL "https://raw.githubusercontent.com/alireza0/iptv/master/sh/hunt.sh" | bash || \
          curl -sL "https://raw.githubusercontent.com/yousif-iptv/hunter/main/hunt.sh" | bash
